# Neuro MCP relay & registry

A permissions-based, controllable MCP relay and pseudo-registry.

## Running the server

You can run this server simply by running `node .output/server/index.mjs`.
This will start the server on port 3000.

## Usage

Please see the docs for usage info.
